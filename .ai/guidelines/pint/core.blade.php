# Laravel Pint Code Formatter

- If you have modified any PHP files, run `composer format` before finalizing changes so the code matches the project's expected style.
- Use Composer scripts as the tooling entry point; do not invoke Pint from `vendor/bin` directly.
